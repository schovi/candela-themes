# Doc style

- One job per file. If two topics drift apart, split them.
- Cross-link, don't duplicate. Facts live in one place; other docs point at it.
- No execution logs. Git history records what happened; docs record what is true now.

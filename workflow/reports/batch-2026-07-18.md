# Batch Run: 2026-07-18

Status: complete

Tasks: 021, 022, 023
Dropped: none

Order rationale: explicit IDs in given order. 022 and 023 both `waits: 021`;
running 021 first satisfies both. No external/ungroomed deps.

## Environmental note (resolved after 021)

During the 021 run, an Orca auto-commit hook (`~/.orca/agent-hooks/claude-hook.sh`,
global PostToolUse) auto-committed the working tree with generated messages,
producing a stray `groom: 020` commit (`9d0cdd8`, a coherent 152-line re-groom of
task 020's spec — NOT in this batch) and a mis-prefixed `021:` commit the worker
soft-reset and folded into the atomic `task 021:` commit. Resolution (user):
Orca hooks removed, so no interference for 022/023; the `groom: 020` commit is
kept intentionally (flagged in Needs manual review for a separate 020 review).

## Per task

### 021 — Explorer as a public site: multi-page shell + homepage
- **Status**: done
- **Duration**: ~16 min (dispatch→return) · **Verification depth**: typecheck+build (vite/tsc green), theme validators green, Playwright screenshots byte-identical (16 cards), Chrome-MCP visual pass on /, /themes, /lab; no unit/e2e surface exists · **Sub-agents**: 1 (acceptance-verifier) · **Tests added**: 0
- **Acceptance verdict**: ready (all 7 criteria PASS)
- **Key decisions**: D1 logged — static multi-page (no SPA/router), Vite middleware rewrites clean URLs to `.html` in dev/preview to match Cloudflare; light/dark split from `tone.includes('dark')` (14 light + 2 dark = 16); `*.entry.tsx` naming to avoid macOS case collision.
- **Files changed**: `app/index.html`, new `app/themes.html`+`app/lab.html`, `app/vite.config.ts`, new `app/src/{SiteShell,Home,ShotView,home.entry,themes.entry,lab.entry}.tsx`, modified `app/src/{ThemeCard,themes}.tsx`+`styles.css`, deleted `app/src/{App,main}.tsx`, `README.md`, `AGENTS.md`, `docs/decisions.md`. Commit `52f4778`.
- **Validation**: all gates green.
- **Issues**: Orca auto-commit hook interfered (see Environmental note); worker recovered with soft-reset + `--no-verify`, left a clean tree and correct atomic commit.

### 022 — Themes gallery: search, filters, per-theme anchors
- **Status**: done
- **Duration**: ~9 min (dispatch→return) · **Verification depth**: typecheck+build green, JSON + `validate.js` gates green (new `mode` gate confirmed failing on bad input), Chrome-MCP live drive of all filters + anchor; no unit/e2e harness in repo · **Sub-agents**: 1 (acceptance-verifier) · **Tests added**: 0
- **Acceptance verdict**: ready (all 6 criteria PASS)
- **Key decisions**: added explicit `mode` field to all 16 themes (14 light + 2 dark) with a `lib/rules.js` gate, replacing 021's `tone.includes('dark')` heuristic; anchor scroll waits on `document.fonts.ready` before `scrollIntoView` (fonts reflow tall sample panes).
- **Files changed**: `themes/aurora-themes.json` (mode ×16), `lib/rules.js`, `app/src/{themes.ts,Gallery.tsx (new),themes.entry.tsx,styles.css,derive.ts,Playground.tsx}`, `AGENTS.md`, `README.md`. Commit `9d2211a`.
- **Validation**: all gates green (7 pre-existing non-gating accent-hue warnings).
- **Issues**: none (hook removed; atomic commit landed clean, no stray commits).

### 023 — The Lab: rename & recopy Playground + Guided
- **Status**: done
- **Duration**: ~4 min (dispatch→return) · **Verification depth**: typecheck+build green + bundle-string grep confirming new labels/anchors shipped and no stale switcher refs; Chrome-MCP/unit skipped (pure UI/content wrap, no runnable logic, task gated only by app build) · **Sub-agents**: 1 (acceptance-verifier) · **Tests added**: 0
- **Acceptance verdict**: ready (all 4 criteria PASS)
- **Key decisions**: Playground→**Theme Editor** (`#theme-editor`), Guided→**Theme Builder** (`#theme-builder`); removed the tool `<select>` switcher and render both tools stacked in anchored sections with a landing blurb linking to each; each tool owns its heading+intro so `lab.entry.tsx` stays thin.
- **Files changed**: `app/src/{lab.entry.tsx,Playground.tsx,Guided.tsx,styles.css}`, `README.md`, `AGENTS.md`. Commit `de88219`.
- **Validation**: `npm run build` green; bundle grep confirms both new names + anchors, zero stale `lab-controls`/`Playground`/`Guided` UI refs.
- **Issues**: none.

## Overall

| Metric | Value |
|--------|-------|
| Completed | 3/3 |
| Stopped (failed/partial/re-groom) | 0 |
| Not run | 0 |
| Total wall time | ~29 min (16 + 9 + 4; excludes mid-batch pause for hook decision) |

### Files touched by multiple tasks
- `app/src/styles.css` — 021 (shell/home), 022 (filter bar), 023 (lab layout)
- `app/src/lab.entry.tsx` — created 021, rewritten 023
- `app/src/Playground.tsx` — 022 (`mode` type), 023 (Theme Editor wrap)
- `README.md`, `AGENTS.md` — all three (docs sync)
- `app/src/themes.ts` — 021 (theme list), 022 (`mode` field)

### Needs manual review
- **Stray `groom: 020` commit `9d0cdd8`** — an Orca auto-commit hook (since removed) committed a coherent 152-line re-groom of task 020's spec (Cloudflare-MCP provisioning + validation-gated publish) that was NOT part of this batch. User chose to keep it. Review task 020's re-groomed spec independently before working it.

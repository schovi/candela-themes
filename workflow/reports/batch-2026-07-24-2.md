# Batch Run: 2026-07-24 (2)

Status: stopped | at=046

Tasks: 046, 048, 049, 050, 052
Dropped: none

Order rationale: explicit IDs in given order; 049 depends: 048 (satisfied — 048 runs first, before 049).

## Per task

### 046 — Establish secure, contributor-ready GitHub defaults
- **Status**: partial
- **Duration**: ~18 min (dispatch→return) · **Verification depth**: JSON validity + `node scripts/validate.js` (exit 0) + `npm run build` + workflow/template YAML parse + lockfile sanity + live `gh api` reads. Explorer build & Chrome-MCP/e2e skipped (non-UI, no `app/` changes). No green CI run yet for the workflow edits. · **Sub-agents**: 2 · **Tests added**: 0
- **Acceptance verdict**: not ready — 4 PASS (criteria 2,3,5,6), 1 FAIL (criterion 1, `main` protection), 1 partial/unverifiable (criterion 4: `v*` tag immutability applied; release-object immutability has no REST endpoint). Fresh-context acceptance-verifier confirmed.
- **Key decisions**:
  - SHA-pinned actions to the currently-used v4 major commit SHAs (behavior-preserving), not an upgrade to latest majors.
  - `main` branch protection deferred (logged D8): a user-owned repo cannot grant the GitHub Actions actor a ruleset bypass (`422`), and a bypass-less ruleset breaks the CI release push. Deleted the release-breaking test ruleset.
  - Tag protection via `v*` tag ruleset (no bypass needed). `publish-marketplaces` dispatched against the tag ref.
- **Files changed**: `.github/workflows/{ci,release,publish-marketplaces}.yml`; `package.json`, `package-lock.json` (added `ovsx`); `.github/dependabot.yml` (new); `.github/{CONTRIBUTING,SECURITY,CODE_OF_CONDUCT}.md`, `PULL_REQUEST_TEMPLATE.md`, `ISSUE_TEMPLATE/{config.yml,bug_report.yml,theme_or_format_request.yml}` (all new); `README.md`; `docs/release-runbook.md`; `docs/decisions.md` (D8). Plus live GitHub settings (Actions allowlist, Dependabot, CodeQL, private vuln reporting, `marketplace` environment, `v*` tag ruleset, topics).
- **Validation**: JSON valid; validate.js exit 0; build clean; YAML parses; `ovsx` in lockfile.
- **Committed**: `99bcaff` (task left in `workflow/in-progress/`, no `done:` date).
- **Issues (needs manual review)**:
  1. `main` branch protection (criterion 1) — platform-blocked. Human decision needed among: (a) move repo into an org, (b) release-PR model, (c) fine-grained PAT as admin bypass actor. See D8 + task Notes.
  2. Immutable releases (part of criterion 4) — no REST endpoint on this account; toggle once by hand in repo Settings. `v*` tag ruleset already blocks tag update/deletion.

### 048 — Simple-mode edits touch only their own tokens
- **Status**: not run

### 049 — Dark theme authoring in both editor modes
- **Status**: not run

### 050 — Retune Sepia Paper to six distinct accent hues
- **Status**: not run

### 052 — Stop swallowing the first click after page load
- **Status**: not run

## Overall

| Metric | Value |
|--------|-------|
| Completed | 0/5 |
| Stopped (failed/partial/re-groom) | 1 (046 partial) |
| Not run | 4 |
| Total wall time | ~18 min |

### Files touched by multiple tasks
None (only 046 ran).

### Needs manual review
- **046** — two blockers requiring a human decision before the task can reach `done`:
  1. `main` branch protection mechanism (org move / release-PR / PAT bypass actor). Blocks acceptance criterion 1. Logged as decision D8.
  2. Immutable-releases toggle has no REST endpoint — flip once by hand in repo Settings.

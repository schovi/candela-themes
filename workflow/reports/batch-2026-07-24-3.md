# Batch Run: 2026-07-24 (run 3)

Status: complete

Tasks: 048, 049, 050, 052
Order: 048 → 049 → 050 → 052 (explicit)
Dependencies: 049 waits: 048 — satisfied by order (048 runs first)
Dropped: none

## Per task

### 048 — Simple-mode edits touch only their own tokens
- **Status**: done
- **Duration**: ~12 min · **Verification depth**: typecheck+build (app tsc/vite/prerender), unit (app node --test 4/4), root build, validate.js (16 themes pass); no Chrome-MCP — pure state-transform logic covered by typecheck + trace · **Sub-agents**: 1 · **Tests added**: 0 (harness can't import derive.ts; matches repo convention)
- **Acceptance verdict**: ready (7/7 PASS, fresh-context gate)
- **Key decisions**: targeted edits reuse existing per-token oracles (not new formulas); `applyGuidedEdit` re-applies helper values on new baseline so palette-helper adjustments persist; extracted shared `backgroundShades`; renamed background-step slider label. No new decision logged (implements D2).
- **Files changed**: `app/src/derive.ts` (applyBackground/applyAccentHue/applyDiagnosticHue, backgroundShades extraction, invalid-hex guard, PUNCT_SAT export); `app/src/Playground.tsx` (applyGuidedEdit + setMood/setDarkness/setAccentHue/setDiagnosticHue, label rename, 4 call sites)
- **Validation**: all green
- **Issues**: byte-identical token isolation holds from clean state; with non-zero palette-helper sliders, re-applying helpers can shift other tokens (intended cost of criterion 4). Mood/darkness edit doesn't re-fit accents vs new bg — extreme changes can surface an AA warning for the user (consistent with spec).

### 049 — Dark theme authoring in both editor modes
- **Status**: done
- **Duration**: ~15 min · **Verification depth**: typecheck+build (app tsc/vite), unit 11/11 (7 new + 4 existing), validate.js 16 themes pass, root build clean, behavioral checkTheme sweep (3 hues × 3 moods, round-trips, mode flips, autofix); no Chrome-MCP — hard invariants proven via checkTheme, eyeball is warn-only · **Sub-agents**: 1 · **Tests added**: 7
- **Acceptance verdict**: ready (5/5 PASS, fresh-context gate)
- **Key decisions**: darkness slider is two-band map (light 0.94→0.82, dark 0.28→0.10) with deliberate mid-luminance gap; mode derives from bg lightness (modeForBg, single source); applyBackground refits full foreground only on light↔dark flip, same-mode nudges keep 048's byte-identical isolation; autofix guard clamps fitLightness bg search to the mode's side of 0.5; export/validation needed no new code.
- **Files changed**: app/src/derive.ts, app/src/autofix.ts, app/src/Playground.tsx, app/src/derive.test.ts (new), app/scripts/run-tests.mjs (new), app/package.json, README.md
- **Validation**: all green
- **Issues**: added `app/scripts/run-tests.mjs` (esbuild bundle → node --test) to resolve the app's extensionless imports so `npm test` works — this is the harness limitation 048 flagged, now fixed. Minor non-gated follow-up: dark fitter may settle `selection` slightly darker than `bg` to clear ink-on-selection AA; rules-valid, left for eyeball tuning.

### 050 — Retune Sepia Paper to six distinct accent hues
- **Status**: done
- **Duration**: ~5 min · **Verification depth**: JSON validity, validate.js (16 themes pass, sepia no longer warns: 7→6 warnings), package build, app build, live Chrome-MCP eyeball of editor Validation pane; no typecheck/unit — JSON-source project has no such gates · **Sub-agents**: 1 · **Tests added**: 0 (validator is the check)
- **Acceptance verdict**: ready (3/3 PASS, fresh-context gate)
- **Key decisions**: retuned palette not rule; kw (h26) + num (h32) shared a 30° bucket collapsing count to 5; moved the rarer `num` (h32→h49 golden-olive #925e21→#6d5e1a), s0.61 (no neon), AA 5.46:1 vs bg, distinct from diagnostics. distinctAccentHues now 6.
- **Files changed**: `themes/candela-themes.json` (one line, sepia-paper num)
- **Validation**: all green; sepia dropped out of warn list
- **Issues**: non-blocking follow-up — committed gallery screenshot `docs/screenshots/candela-sepia-paper.png` still shows old num color; regeneration was outside task's JSON+README boundary. Live gallery/editor read JSON and are correct.

### 052 — Stop swallowing the first click after page load
- **Status**: done
- **Duration**: ~6 min · **Verification depth**: typecheck+build (app tsc/vite/prerender, dist pages keep prerendered content + guard); no unit/e2e (app has no test suite; deterministic DOM-marker toggle); no Chrome-MCP — sub-second pre-hydration race not reproducible on demand, mechanism verified by inspection + dist output · **Sub-agents**: 1 · **Tests added**: 0 (no framework; build+prerender is the check)
- **Acceptance verdict**: ready (2/2 PASS, fresh-context gate)
- **Key decisions**: root cause is the bundle-download/hydration window, so inert marker ships in HTML via inline classic `<script>` (runs before deferred module), placed after empty `#root` to preserve prerender EMPTY_ROOT match and keep no-JS nav clickable; chose inert approach (pointer-events:none + aria-busy + cursor:progress) over click-queuing; marker lifted in `HydrationGate` useEffect; guard routed through unified `mountOrHydrate` for all three pages.
- **Files changed**: app/src/mount.tsx, app/src/editor.entry.tsx, app/src/styles.css, app/index.html, app/themes.html, app/editor.html
- **Validation**: `npm run build` green (tsc, vite, prerender); dist confirmed prerendered roots + SEO meta retained, guard present on all three pages
- **Issues**: none. Accepted tradeoff: if the JS bundle fails to load entirely, root stays inert (no-JS users unaffected — marker is JS-set).

## Overall

| Metric | Value |
|--------|-------|
| Completed | 4/4 |
| Stopped (failed/partial/re-groom) | 0 |
| Not run | 0 |
| Total wall time | ~38 min |

### Files touched by multiple tasks
- `app/src/derive.ts` — 048, 049
- `app/src/Playground.tsx` — 048, 049
- (049 built on 048's targeted-edit foundation; the `depends: 048` order held.)

### Needs manual review
Non-blocking follow-ups (none gate completion):
- 049 added `app/scripts/run-tests.mjs` (esbuild → node --test) to fix the app's extensionless-import test harness — the limitation 048 flagged, now resolved.
- 050 left the committed gallery screenshot `docs/screenshots/candela-sepia-paper.png` showing the old num color; regenerate with `npm run app:screenshots` when convenient (live gallery/editor read JSON and are already correct).
- 049 minor: dark fitter may settle `selection` slightly darker than `bg`; rules-valid, left for eyeball tuning.
